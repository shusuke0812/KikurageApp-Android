import {width} from '../lib/window';

const edgeNumber = 2;
const ratio = 3;
const customRatio = edgeNumber / ratio;

export const BUTTON_WIDTH = width * customRatio;
export const LOGO_WIDTH = width * customRatio;
